WebReg Revival — static build (browser-only, no server).
Served by GitHub Pages. All Fall 2026 data is baked into data/catalog.json;
each visitor's planned schedule lives in their own browser (localStorage).
Rebuild after a data refresh: python3 scripts/export_static.py
